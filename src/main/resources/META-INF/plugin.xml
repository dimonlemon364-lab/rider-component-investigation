<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.component-investigation.component-investigation</id>
    <!-- name & vendor are provided by the Gradle build (intellijPlatform.pluginConfiguration). -->

    <description><![CDATA[
        <b>Show Relations for Component/Class</b> — adds a <b>Relations</b> code-vision link next to
        <em>Usages</em>/<em>Inheritors</em> on any C#/F#/VB type. Click it to see, in the native Find
        Usages view, every place the class is used <em>and how</em>: usages of the class itself
        (instantiation, type usage) and of each member (methods, properties, fields, constants,
        events), grouped by member and by access type (read / write / invocation / instantiation).<br>
        Toggle the results between a folder tree and a flat list, and use <b>Export to Markdown</b> to
        write the full report — with a real usage example per call site and clickable file links.
        ]]></description>

    <!-- Rider-only: the plugin ships a ReSharper backend and needs the .NET analysis. -->
    <depends>com.intellij.modules.rider</depends>

    <!-- Markdown integration (link navigation in exported reports) is optional: the plugin still
         loads if the Markdown plugin is disabled. -->
    <depends optional="true" config-file="component-investigation-markdown.xml">org.intellij.plugins.markdown</depends>

    <extensions defaultExtensionNs="com.intellij">
        <usageGroupingRuleProvider implementation="com.componentinvestigation.RelationGroupingRuleProvider"/>
        <notificationGroup id="Component Investigation" displayType="BALLOON"/>
        <postStartupActivity implementation="com.componentinvestigation.RelationsProtocolListener"/>
    </extensions>

</idea-plugin>
